// Three.js mesh renderer interop
// This file will contain JS functions for:
// - init(canvasId, options): Setup scene, camera, controls
// - loadMesh(geometry, groups?, materialOpts?): Build BufferGeometry from typed arrays
// - updateMaterial(meshId, materialOpts): Change color, wireframe, etc.
// - clear() / dispose(meshId?): Cleanup resources
// - resize(width, height): Update viewport and camera

