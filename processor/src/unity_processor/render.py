from pathlib import Path


def render_preview_images(parsed: dict, output_dir: Path) -> list[str]:
    """Placeholder render function for Phase 1."""
    output_dir.mkdir(parents=True, exist_ok=True)
    return []
